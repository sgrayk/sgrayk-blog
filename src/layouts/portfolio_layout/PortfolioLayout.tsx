import { Box } from "@mui/material";

const PortfolioLayout = () => {
  return (
    <Box
      sx={{
        flex: 1,
        height: "100%",
      }}
    >
      <p>Portfolio layout</p>
    </Box>
  );
};

export default PortfolioLayout;
